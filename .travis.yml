language: php

php:
    - 5.4
    - 5.5
    - 5.6
    - 7.0
    - hhvm
    - hhvm-nightly

sudo: false

cache:
    directories:
        - $HOME/.composer/cache

matrix:
    include:
        - php: 5.4
          env: DEPENDENCIES='low'
        - php: 5.6
          env: DEPENDENCIES='dev'
    allow_failures:
        - php: 7.0
        - php: hhvm-nightly
        - env: DEPENDENCIES='dev'
    fast_finish: true

install:
    - export COMPOSER_ROOT_VERSION=dev-master
    - if [ "$DEPENDENCIES" == "dev" ]; then perl -pi -e 's/^}$/,"minimum-stability":"dev"}/' composer.json; fi;
    - if [ "$DEPENDENCIES" != "low" ]; then travis_retry composer update --prefer-source; fi;
    - if [ "$DEPENDENCIES" == "low" ]; then travis_retry composer update --prefer-source --prefer-lowest; fi;

script:
    - bin/phpunit --configuration phpunit.xml.dist
